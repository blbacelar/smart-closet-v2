# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Agent workflow

Use the installed ECC plugin and its skills as the default engineering workflow for this project. Do not use BMAD skills unless the user explicitly requests BMAD.
